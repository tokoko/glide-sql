[project]
name = "glide-sql"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "pyarrow",
    "uvicorn",
    "duckdb==1.2.2",
    "requests",
    "substrait[extensions]",
    "pandas"
]
